extension ExtendedObject on Object {
  dynamic fromJson(dynamic json) {
    throw UnimplementedError("Don't use the placeholder methods");
  }

  dynamic toJson() {
    throw UnimplementedError("Don't use the placeholder methods");
  }
}
