import 'package:flutter/material.dart';

class InputFromPage extends StatelessWidget {
  const InputFromPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

enum FormType { idForm, phoneNumberForm, sarsReform }
