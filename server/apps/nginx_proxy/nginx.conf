events {}
http {
    upstream fb_notifications_service {
        server fb_notifications_service:4309;
    }

    upstream payments_service {
        server payments_service:5436;
    }

    upstream store_service {
        server store_service:5400;
    }

    upstream verify_id_service {
        server verify_id_service:5434;
    }

    # Redirect HTTP to HTTPS
    # server {
    #     listen 80;
    #     listen [::]:80;
    #     server_name verified.io www.verified.io;

    #     ignore_invalid_headers off;
    #     underscores_in_headers on;

    #     location / {
    #         return 301 https://$host$request_uri;
    #     }

    # }

    server {
        listen 2202;
        listen [::]:2202;
        server_name localhost;

        ignore_invalid_headers off;
        underscores_in_headers on;


        location /notifications/ {
            proxy_pass http://fb_notifications_service/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_pass_request_headers on;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_pass_header Server;
            proxy_cache_bypass $http_upgrade;
            proxy_redirect off;
        }

        # payments_service
        location /payments/ {
            proxy_pass http://payments_service/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_pass_request_headers on;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_pass_header Server;
            proxy_cache_bypass $http_upgrade;
            proxy_redirect off;
        }

        # store_service
        location /store/ {
            proxy_pass http://store_service/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_pass_request_headers on;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_pass_header Server;
            proxy_cache_bypass $http_upgrade;
            proxy_redirect off;
        }

        # verify_id_service
        location /verify-id/ {
            proxy_pass http://verify_id_service/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_pass_request_headers on;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_pass_header Server;
            proxy_cache_bypass $http_upgrade;
            proxy_redirect off;
        }
    }

    # HTTPS server
    # server {
    #     listen 4431;
    #     listen [::]:4431;
    #     server_name verified.io www.verified.io;

    #     ignore_invalid_headers off;
    #     underscores_in_headers on;

    #     # # SSL certificate files
    #     # ssl_certificate /server/apps/nginx_proxy/verified_io.crt;
    #     # ssl_certificate_key /server/apps/nginx_proxy/verified_io.key;

    #     # Recommended SSL settings
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers EECDH+AESGCM:EDH+AESGCM;
    #     ssl_prefer_server_ciphers on;
    #     ssl_session_cache shared:SSL:10m;

    #     # OCSP Stapling
    #     ssl_stapling on;
    #     ssl_stapling_verify on;
    #     resolver 8.8.8.8 8.8.4.4 valid=300s;
    #     resolver_timeout 5s;

    #     # HSTS (optional)
    #     # add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;

    #     # fb_notifications_service
    #     location /notifications/ {
    #         proxy_pass http://fb_notifications_service/;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #         proxy_pass_request_headers on;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection 'upgrade';
    #         proxy_pass_header Server;
    #         proxy_cache_bypass $http_upgrade;
    #         proxy_redirect off;
    #     }

    #     # payments_service
    #     location /payments/ {
    #         proxy_pass http://payments_service/;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #         proxy_pass_request_headers on;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection 'upgrade';
    #         proxy_pass_header Server;
    #         proxy_cache_bypass $http_upgrade;
    #         proxy_redirect off;
    #     }

    #     # store_service
    #     location /store/ {
    #         proxy_pass http://store_service/;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #         proxy_pass_request_headers on;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection 'upgrade';
    #         proxy_pass_header Server;
    #         proxy_cache_bypass $http_upgrade;
    #         proxy_redirect off;
    #     }

    #     # verify_id_service
    #     location /verify-id/ {
    #         proxy_pass http://verify_id_service/;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #         proxy_pass_request_headers on;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection 'upgrade';
    #         proxy_pass_header Server;
    #         proxy_cache_bypass $http_upgrade;
    #         proxy_redirect off;
    #     }
    # }
}

